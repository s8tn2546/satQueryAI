# Training-only dependencies for the Remote-Sensing VLM LoRA fine-tune
# (adaptation/train_lora_rsvqa.py).
#
# These are the packages actually imported by the training script and are NOT
# runtime service dependencies. Install separately from the runtime
# requirements so the serving image stays lean:
#
#   pip install -r requirements-training.txt
#
# Note: torch/torchvision are pinned loosely to match the runtime
# requirements.txt range and must expose a CUDA build for GPU training.

# Core deep-learning stack
torch>=2.0.0,<3.0.0
torchvision>=0.15.0

# Hugging Face model + PEFT (LoRA) stack
transformers>=5.0.0,<6.0.0
peft>=0.5.0,<1.0.0
accelerate>=0.20.0

# Qwen2-VL chat template / vision info processing
qwen-vl-utils>=0.0.1

# Dataset loading (streaming RSVQA-HR)
datasets>=2.14.0

# Image handling (PIL) is required for RSVQA image preprocessing
Pillow>=10.0.0,<11.0.0
